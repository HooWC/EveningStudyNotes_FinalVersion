```
git config --global --add safe.directory 'D:/本地/个人/test'
```

```
// 第一次 Upload 新项目的时候

git init
git branch -M main // 将 Master 换成 Main 名称
git remote add origin https://gitlab.com/xxx/xxx.git // 换成你自己的
git pull --rebase // 拉取 Main 最新的状态

git add .
git commit -m "first commit"
git push -u origin main
```

```
// 更新

git status
git add .
git commit -m "up"
git push -u origin main
```

