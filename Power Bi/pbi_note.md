```
Total Sales = SUM(Sales[Amount])
```

```
SUM(Table[Column])                 -- 求和
AVERAGE(Table[Column])             -- 平均值
MIN(Table[Column])                 -- 最小值
MAX(Table[Column])                 -- 最大值
COUNT(Table[Column])               -- 计数
DISTINCTCOUNT(Table[Column])       -- 不重复计数
```

```
Ratio = DIVIDE([Male], [Total Emp], 0)   -- 除法
```

```
C# ASP NET MVC
HTML
CSS
JAVASCRIPT
JQUERY
TAILWINDCSS
BOOTSTRAP
REACT
VUE3
REA
```

